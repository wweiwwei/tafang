export interface IBattleRoad {
    init(pos: number[], scale: number, data: number[][]): void;

    getTotalLength(): number;

    getPointAt(offset: number): {
        pos: { x: number; y: number };
        direction: { x: number; y: number };
    };
}
