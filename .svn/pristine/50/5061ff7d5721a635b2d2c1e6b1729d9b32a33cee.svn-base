import UIComponent from "./UIComponent";

const { ccclass, property } = cc._decorator;

@ccclass
export default abstract class UIListItem extends UIComponent {
    /** item的状态 */
    abstract state: any;
    abstract setState(state: this["state"]): void;
}
